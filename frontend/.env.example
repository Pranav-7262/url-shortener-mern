# Frontend Vite env example
# For production, set VITE_API_URL to your backend's base URL (no trailing slash)
# For example: VITE_API_URL=https://your-backend.onrender.com
VITE_API_URL=
# Optional: local dev backend override used by vite proxy if you want to change it
# BACKEND_URL=http://localhost:3000
# Optional: cookie domain rewrite for dev proxy
# COOKIE_DOMAIN_REWRITE=localhost
